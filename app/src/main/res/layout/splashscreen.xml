<?xml version="1.0" encoding="utf-8"?>
<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:animateLayoutChanges="true">

    <ImageView
        android:id="@+id/image_view_1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layoutAnimation="@animator/fade_in"
        android:layoutAnimationInterpolator="@android:anim/accelerate_interpolator"
        android:src="@drawable/splash" />

    <ImageView
        android:id="@+id/image_view_2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:alpha="0"
        android:layoutAnimation="@animator/fade_out"
        android:layoutAnimationInterpolator="@android:anim/decelerate_interpolator"
        android:src="@drawable/bg" />

</RelativeLayout>